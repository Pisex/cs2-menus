"Core"
{
    "ServerLang"	        "ru"
    "MenuType"              "0"     //Default menu type || Тип меню по умолчанию
                                    //0 - Chat
                                    //1 - HTML Center
                                    //2 - HTML Center with WASD
    "MenuFlashFix"          "0"     //Fix for flash in the menu(possibly unstable) || Фикс мигания в меню(возможно нестабильно)
                                    //0 - Disable
                                    //1 - Enable
    "MenuChangeCommand"     "!menu" //Команда для открытия меню редактирования типа меню || Command to open the menu for editing the menu type
    "AccessUserChangeType"  "1"     //Давать ли пользователю возможность менять тип меню(MenuChangeCommand) || Can the user change the menu type(MenuChangeCommand)
                                    //0 - Disable
                                    //1 - Enable
    "MenuAddon"             "0"     //Откуда будут браться визуальный кнопки для меню || Where the visual buttons for the menu will be taken from
                                    // Только для MenuType == 2  || Only for MenuType == 2
                                    // 0 - Site URL
                                    // 1 - Addon
    "StopingUser"           "0"     //Блокировать ли игроку передвижение при открытом меню || Block player movement when the menu is open
                                    // Только для MenuType == 2  || Only for MenuType == 2
                                    // 0 - Disable
                                    // 1 - Enable
    "MenuURL"               "pisex.online/module/buttons"// Только для MenuType == 2  || Only for MenuType == 2
    "MenuTime"              "120"   //Menu auto-close time

    "TimeoutInputMenu"      "200"   // Только для MenuType == 2  || Only for MenuType == 2
                                    // Задержка перед следующим нажатием клавиши меню в миллисекундах || Delay before the next key press in the menu in milliseconds

    "delay_auth_fail_kick"  "30"    //After how many seconds to kick an unauthorized player, 0 - no kicking

    "commands_eater"        "!,/"   // С каким префиксом команды не будут отображаться в чате (если она существует) через запятую ||
                                    // With what prefix the commands will not be displayed in the chat (if it exists) separated by commas
}