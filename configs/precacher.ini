"Precacher"
{
    // Ключ (имя) не важен, используется только значение (путь к ресурсу)
    // The key name does not matter, only the value (resource path) is used
    //
    // Примеры || Examples:
    // "1"    "characters/models/my_character/my_character.vmdl"
    // "2"    "particles/my_effect/my_effect.vpcf"
    // "3"    "soundevents/my_soundevents.vsndevts"
}
